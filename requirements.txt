datasets==5.0.0
hydra-core==1.3.3
numpy==1.22.4
omegaconf==2.3.1
peft==0.19.1
torch==1.10.0
transformers==4.10.0
wandb==0.28.0
accelerate

# Figures + manifest (evals/plot_*.py, evals/manifest.py). Plotting needs no
# torch, so these alone are enough to re-render a figure with --plot-only.
matplotlib
pyyaml

# Gemini judge used by the refusal / counterfactual / simulatability evals
# (evals/judges.py). Needs ADC: `gcloud auth application-default login` plus
# GOOGLE_CLOUD_PROJECT.
google-genai
tqdm
