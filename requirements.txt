# Python >= 3.10.
#
# Training stack. transformers must be >= 4.43: that is the release that added
# Llama 3.1 support, and earlier versions have no llama architecture at all, so
# they cannot load the base model these configs point at.
torch>=2.1
transformers>=4.43
peft>=0.11
accelerate>=0.33
datasets>=2.20
numpy>=1.24,<3
hydra-core>=1.3
omegaconf>=2.3
wandb>=0.16        # optional — logging is skipped if the import fails

# Figures + manifest (evals/plot_*.py, evals/manifest.py). Plotting needs no
# torch, so these alone are enough to re-render a figure with --plot-only.
matplotlib>=3.7
pyyaml>=6

# Gemini judge used by the refusal / counterfactual / simulatability evals
# (evals/gemini.py). Needs ADC: `gcloud auth application-default login` plus
# GOOGLE_CLOUD_PROJECT.
google-genai>=1.0
tqdm>=4.66
